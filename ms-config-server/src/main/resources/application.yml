server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hokjose/mi_microservicio_pastel.git
          search-paths: config-data
          #username
          #password:
          default-label: ms-config-server
  security:
    user:
      name: root
      password: s3cr3t
